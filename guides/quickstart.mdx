---
title: "Quick start"
description: "Get up and running with Pan API in under 5 minutes"
---

This guide will walk you through your first API call to Pan. By the end, you'll have created a wallet and checked its balance.

<Steps>
  <Step title="Get your API key">
    Sign up at [app.pan.dev](https://app.pan.dev) and navigate to your dashboard. Copy your API key from the settings page.
    
    <Warning>
      Keep your API key secret. Never expose it in client-side code or public repositories.
    </Warning>
  </Step>
  
  <Step title="Make your first request">
    Create a wallet for a user in your application:
    
    <CodeGroup>
    ```bash cURL
    curl -X POST https://api.pan.dev/v1/wallets \
      -H "Authorization: Bearer YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "userId": "user_123",
        "metadata": {
          "app": "my-app"
        }
      }'
    ```
    
    ```javascript JavaScript
    const response = await fetch('https://api.pan.dev/v1/wallets', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${process.env.PAN_API_KEY}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        userId: 'user_123',
        metadata: {
          app: 'my-app'
        }
      })
    });
    
    const wallet = await response.json();
    console.log(wallet);
    ```
    
    ```python Python
    import requests
    import os
    
    response = requests.post(
        'https://api.pan.dev/v1/wallets',
        headers={
            'Authorization': f"Bearer {os.getenv('PAN_API_KEY')}",
            'Content-Type': 'application/json'
        },
        json={
            'userId': 'user_123',
            'metadata': {
                'app': 'my-app'
            }
        }
    )
    
    wallet = response.json()
    print(wallet)
    ```
    </CodeGroup>
    
    <ResponseExample>
    ```json Success response
    {
      "id": "wallet_abc123",
      "userId": "user_123",
      "address": "0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb",
      "chains": ["ethereum-sepolia", "base-sepolia", "arbitrum-sepolia"],
      "metadata": {
        "app": "my-app"
      },
      "createdAt": "2024-01-15T10:30:00Z"
    }
    ```
    </ResponseExample>
  </Step>
  
  <Step title="Check wallet balance">
    Query the balance of your newly created wallet:
    
    <CodeGroup>
    ```bash cURL
    curl -X GET https://api.pan.dev/v1/balances/wallet_abc123 \
      -H "Authorization: Bearer YOUR_API_KEY"
    ```
    
    ```javascript JavaScript
    const response = await fetch(
      'https://api.pan.dev/v1/balances/wallet_abc123',
      {
        headers: {
          'Authorization': `Bearer ${process.env.PAN_API_KEY}`
        }
      }
    );
    
    const balances = await response.json();
    console.log(balances);
    ```
    
    ```python Python
    response = requests.get(
        'https://api.pan.dev/v1/balances/wallet_abc123',
        headers={
            'Authorization': f"Bearer {os.getenv('PAN_API_KEY')}"
        }
    )
    
    balances = response.json()
    print(balances)
    ```
    </CodeGroup>
    
    <ResponseExample>
    ```json Success response
    {
      "walletId": "wallet_abc123",
      "chains": [
        {
          "chain": "ethereum-sepolia",
          "tokens": [
            {
              "asset": "USDC",
              "balance": "1000000000",
              "balanceFormatted": "1000.0",
              "decimals": 6,
              "valueUsd": 1000.00
            }
          ]
        },
        {
          "chain": "base-sepolia",
          "tokens": []
        },
        {
          "chain": "arbitrum-sepolia",
          "tokens": []
        }
      ],
      "totalValueUsd": 1000.00
    }
    ```
    </ResponseExample>
  </Step>
  
  <Step title="Execute your first intent">
    Try executing a lending intent to deposit funds into a DeFi protocol:
    
    <CodeGroup>
    ```bash cURL
    curl -X POST https://api.pan.dev/v1/intents \
      -H "Authorization: Bearer YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "walletId": "wallet_abc123",
        "action": "lend",
        "amount": "1000",
        "asset": "USDC",
        "chain": "ethereum-sepolia"
      }'
    ```
    
    ```javascript JavaScript
    const response = await fetch('https://api.pan.dev/v1/intents', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${process.env.PAN_API_KEY}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        walletId: 'wallet_abc123',
        action: 'lend',
        amount: '1000',
        asset: 'USDC',
        chain: 'ethereum-sepolia'
      })
    });
    
    const intent = await response.json();
    console.log('Intent created:', intent.id);
    console.log('Status:', intent.status);
    ```
    
    ```python Python
    response = requests.post(
        'https://api.pan.dev/v1/intents',
        headers={
            'Authorization': f"Bearer {os.getenv('PAN_API_KEY')}",
            'Content-Type': 'application/json'
        },
        json={
            'walletId': 'wallet_abc123',
            'action': 'lend',
            'amount': '1000',
            'asset': 'USDC',
            'chain': 'ethereum-sepolia'
        }
    )
    
    intent = response.json()
    print(f"Intent created: {intent['id']}")
    print(f"Status: {intent['status']}")
    ```
    </CodeGroup>
    
    <Note>
      Intent execution is asynchronous. Poll the intent status endpoint to track progress.
    </Note>
  </Step>
</Steps>

## Next steps

<CardGroup cols={2}>
  <Card
    title="🔐 Authentication guide"
    icon="key"
    href="/guides/authentication"
  >
    Learn about API keys, security best practices, and request signing
  </Card>
  <Card
    title="💼 Core concepts"
    icon="book"
    href="/guides/concepts/overview"
  >
    Deep dive into wallets, intents, and execution plans
  </Card>
  <Card
    title="📖 API reference"
    icon="terminal"
    href="/api-reference/introduction"
  >
    Explore all available endpoints with detailed examples
  </Card>
  <Card
    title="✅ Best practices"
    icon="check-circle"
    href="/guides/best-practices/integration"
  >
    Learn integration patterns and performance optimization
  </Card>
</CardGroup>

<Tip>
  **Testnet tokens**: Remember that Pan currently operates on testnets. Get free testnet tokens from faucets to experiment with the API.
</Tip>

